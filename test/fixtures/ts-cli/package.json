{
  "name": "ts-cli-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal fixture CLI used only to self-test yohn-jp/.github's reusable TypeScript CLI CI workflow (.github/workflows/typescript-cli-ci.yml). Not a published package.",
  "type": "commonjs",
  "bin": {
    "ts-cli-fixture": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "format:check": "echo 'no formatter configured for this fixture; real consumers run e.g. prettier --check .'",
    "lint": "echo 'no linter configured for this fixture; real consumers run e.g. eslint .'",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/*.test.mjs",
    "build": "tsc -p tsconfig.json && chmod +x dist/cli.js",
    "conformance:contract": "node scripts/check-contract.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.4"
}
